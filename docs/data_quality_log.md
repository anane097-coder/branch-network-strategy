# Data Quality Log

Every anomaly found, what was decided, and why. Append-only.

This is a portfolio deliverable. Problems found and handled are evidence of
rigor - do not sanitize this file.

| Date | Source | Issue | Decision | Impact |
|---|---|---|---|---|
| 2026-08-14 | FDIC SOD | Bulk-download ZIP endpoint is dead. `ShowFileWithStats1.asp?strFileName=ALL_<year>.zip` returns HTTP 404 on `www7`, `www5`, and the `dynaDownload.asp` variant. The landing page still resolves, but no ZIP behind it does. | Switched to the FDIC REST API (`api.fdic.gov/banks/sod`), filtered to `STALP:WI/IL AND YEAR:<year>`, paged at 10,000 rows. Verified: 14 state-years, 30,461 branch rows. | **Positive on size** — 18 MB total vs. seven national ZIPs, so raw SOD is comfortably committable. **Negative on narrative** — see DQ-2. |
| 2026-08-14 | FDIC SOD | The API returns a normalized schema across all seven vintages. The schema drift that script 02 exists to profile — field names and codings changing across vintages, handled by a documented column-mapping layer — largely will not be visible through this interface. | Not worked around. Script 02 should be rescoped to profile *content* drift — branch counts, deposit distributions, field population rates by year — rather than *name* drift. | Loses one planned portfolio artifact (`docs/sod_schema_changes.md` as designed). The replacement is honest and still real work. Needs sign-off. |
| 2026-08-14 | FDIC SOD | `STNUMBR` (55) and `CNTYNUMB` (47) come back from the API as **integers**, not zero-padded text. County FIPS must be assembled as `str(STNUMBR).zfill(2) + str(CNTYNUMB).zfill(3)`. | Flagged for script 02, not fixed at download — raw stays raw. | This is trap #1 in `docs/project-guide.md` arriving exactly as predicted. Silent and destructive if missed. |
| 2026-08-14 | FDIC SOD | Illinois branch counts fall 3,133 → 2,801 (2019–2022) then **rise to 3,307 in 2023** before flattening. A +506 single-year increase runs against the national consolidation trend and against Wisconsin's own monotonic decline. | Not resolved. Logged for script 02 to investigate before any trend claim is made. | Would corrupt the BQ-2 deposit-share-decline narrative, and any claim about whether the subject bank consolidated faster or slower than its market, if it is an artifact rather than a real change. |
| 2026-08-14 | HMDA panel | **The RSSD → LEI bridge has no automated source.** The public panel ZIP (`s3.amazonaws.com/cfpb-hmda-public/prod/snapshot-data/<year>/<year>_public_panel_csv.zip`) returns S3 `AccessDenied` for 2023, 2024, and 2025. The Federal Reserve NPW bulk download is behind a CAPTCHA. The FFIEC institutions API exposes an `rssd` field but returns `-1` for every institution tested, including Associated Bank NA (`ZF85QS7OXKPBG52R7N18`). | **Unresolved — this is gate 5.4 and it is blocked.** Downloaded what remains: the full 2025 filer list (LEI + name, 4,789 filers) via `ffiec.cfpb.gov/v2/reporting/filers/2025`. | Confirms *that* a candidate filed (criterion 4, UAT-10) but does not bridge CERT ↔ LEI. Script 04 as designed cannot run. BQ-4 is at risk. Decision needed. |
| 2026-08-14 | HMDA filers | Associated Bank NA returns `hmdaFiler: true` for 2025 from the per-LEI endpoint, but does **not** appear in the `/reporting/filers/2025` list (6 "ASSOCIATED" matches, none of them the bank). Johnson Financial Group — a named candidate subject in 08 §0 — returns **zero** matches. First Business Bank and Waukesha State Bank both appear. | Not resolved. The two endpoints disagree and the filer list may be partial or scoped differently than documented. | Institution selection criterion 4 cannot be applied from the filer list alone until this is understood. |
| 2026-08-14 | HMDA | The endpoint every design doc calls the "Modified LAR" (`ffiec.cfpb.gov/v2/data-browser-api/view/csv`) in fact redirects to `files.ffiec.cfpb.gov/data-browser/datasets/2025/filtered-queries/**snapshot**/…`. It serves the snapshot dataset filtered by state, not the Modified LAR. | Kept the endpoint; corrected the naming in the script and here. The case study must state which file was used. | 05 §2 names confusing these files "the classic HMDA rookie error." Docs currently commit it. Field-level constraints must be re-checked against snapshot documentation, not modified-LAR documentation. |
| 2026-08-14 | Census / catchment | **No CBSA source existed in the pipeline.** `config/catchment.yaml` keys the 3mi/8mi radius rule on CBSA membership, `dim_tract` carries a `cbsa` column, SQL-04 ranks within CBSA and FR-01 filters by it — and nothing downloaded supplied county→CBSA mapping. | Added the OMB/Census delineation file `list1_2023.xlsx` (143 KB, county FIPS → CBSA code, title, metro/micro). 2023 is the current vintage; a 2025 file does not exist. | Unblocks catchment construction (script 08), which everything downstream depended on. |
| 2026-08-14 | Census / LMI | **The LMI flag had no denominator.** LMI is tract median family income below 80% of *area* median family income; only tract-level ACS was being pulled. | Added a CBSA-level ACS pull (`B19013`, `B19113`) and added `B19113_001E` (median **family** income) to the tract pull — the tract request previously carried only `B19013` (median **household** income), which is not the FFIEC definition. | Unblocks `dim_tract.lmi_flag`, AC-06, and BQ-6. The household-vs-family distinction is now an explicit assumption (A-03). |
| 2026-08-14 | FDIC institutions | The pull was filtered to `STALP:WI` / `STALP:IL`, which is **headquarters** state. Out-of-state institutions operating branches in the footprint (Chase, US Bank, BMO) appear in SOD but would have been absent, leaving null `FED_RSSD` and `ASSET` for a large share of competitor certs. | Removed the state filter; now pulls all 27,836 institutions (1.9 MB). `FED_RSSD` confirmed present and populated, returned as quoted text. | Every SOD cert now resolves in `dim_institution`. Affects BQ-1 competitor saturation. |
| 2026-08-14 | manifest.json | Re-running the download overwrote each file's real `retrieved_utc` with the literal string `"pre-existing"`, destroying the provenance the manifest exists to record. | Fixed in `01_download.py`: an already-present file keeps its prior manifest entry when the SHA-256 still matches. | The manifest is a stated portfolio artifact (durability argument, 05 §1). It now survives re-runs. |
| 2026-08-14 | Environment | Only Python 3.14 is installed. `pandas==2.2.3`, `shapely==2.0.6`, and `pyproj==3.7.0` publish no cp314 wheels and cannot install. | **Resolved.** Pins raised to 3.14-compatible versions and verified: imports clean, GEOS reprojection works, geopandas reads the TIGER zip directly (1,542 WI tracts), DuckDB works. | pandas moves 2.2 → **3.0**, a major version. Copy-on-write is now default and the default string dtype is `str`, which helpfully makes GEOID/FIPS coercion to integers less likely. No pandas code predated the change. |
| 2026-08-14 | HMDA | The loan-level file carries `tract_to_msa_income_percentage` and `ffiec_msa_md_median_family_income` — **the FFIEC's own tract-to-area income ratio**, 100% populated. Below 80 is the regulatory definition of an LMI tract. | Adopted as the **primary** basis for `lmi_flag` (assumption A-03 revised). The ACS CBSA pull is retained as the fallback for tracts with no lending activity and as a cross-check. | Better than any ACS reconstruction — it is the figure examiners actually use, which is what a compliance reviewer will expect. Yields 1,428 LMI tracts, 29.9% of tracts with lending. |
| 2026-08-14 | HMDA / TIGER | Tract coverage check across the footprint: 4,772 of 4,807 TIGER 2024 tracts have 2025 lending activity (99.3%). **35 tracts have none** and **1 HMDA tract does not exist in TIGER 2024**. | The 35 need the ACS fallback for `lmi_flag`. The 1 orphan must be identified before the script 07 spatial join — it is either a genuine boundary-vintage difference or a filing error. Logged, not silently dropped. | This is trap #5 (tract vintage mismatch) surfacing early, which is where it is cheap. One bad tract silently dropped is a wrong join nobody notices. |
| 2026-08-14 | ACS / TIGER | Vintage alignment check (trap #5): ACS 2020–2024 returns **4,807** tracts for WI+IL; TIGER 2024 contains **4,807**. Zero in TIGER but not ACS, zero in ACS but not TIGER. All GEOIDs are 11 characters. | No action needed — the two align exactly. | Clears the tract-vintage risk on the ACS↔TIGER join, and isolates the one orphan found earlier as an **HMDA-side** issue specifically rather than a general boundary problem. |
| 2026-08-14 | ACS | Median family income (`B19113`) is suppressed or null for **114 of 4,807** tracts (2.4%) — 42 in WI, 72 in IL. Census returns a large negative sentinel rather than a null. | Treat any negative value as missing, never as an income. Logged for script 05. | A sentinel read as a real number would produce catastrophic negative incomes in the index. Silent and destructive if missed. |
| 2026-08-14 | ACS / HMDA | **AC-06 exception list.** Combining both LMI routes: 4,772 tracts have the FFIEC ratio, 35 need the ACS fallback, and of those **31 have no LMI basis by either route**. Of the 31, **22 have zero population** and carry GEOIDs ending `980000`/`990000` — the Census convention for water-only and special-land-use tracts (Great Lakes shoreline in both states). | The 22 water/unpopulated tracts are correctly excluded from LMI coverage. The remaining **9 populated tracts** (populations 5; 493; 1,128; 1,145; 1,457; 1,934; 2,294; 3,908; 6,568) are enumerated and must be reported as exceptions in AC-06, not silently dropped. | Gives AC-06 the same "zero, or enumerated with reasons" rigor AC-02 already has. 9 named exceptions out of 4,807 is a defensible position; an unexplained gap is not. |
| 2026-08-14 | CBSA delineation | The file lists only counties that belong to a CBSA: **104 rows for WI+IL** against 174 counties in the two states. County FIPS arrives correctly as zero-padded text (`015`). | The ~70 unlisted counties are genuinely non-CBSA and take the non-urban (8mi) radius under A-04. Absence from this file is meaningful, not missing data. | Confirms A-04 is doing real work — roughly 40% of footprint counties are rural and would otherwise get an urban radius by default. |
| 2026-08-14 | ACS | The Census API key is interpolated into request URLs, which are recorded in the manifest. | `01_download.py` redacts it before writing. Verified after a live run: manifest URLs end `key=REDACTED`, and a full-text search of the repository for the key returns nothing. | The manifest is committed to a public repository. An unredacted key would be published. |
| 2026-08-14 | **CORRECTION** | An earlier entry in this log reported that Associated Bank NA returned `hmdaFiler: true` from the per-LEI FFIEC endpoint while being **absent** from `/reporting/filers/2025`, and treated that as an endpoint contradiction. **That was wrong.** The bank is present in both. The error came from truncating a six-row name-match list to four rows and concluding from the visible subset. | Retracted. Tested against the authoritative source instead: LEI `ZF85QS7OXKPBG52R7N18` appears in the 2025 WI+IL loan-level file with **9,260 applications**. There is no contradiction between the endpoints. | The lesson is the finding: a truncated display is not evidence of absence. Where a question can be settled against the loan-level file itself, settle it there rather than against a metadata endpoint. |
| 2026-08-14 | HMDA panel | **RESOLVED — the crosswalk is not blocked.** The earlier entry recorded that no automated RSSD → LEI source existed, because the documented `rssd` field returns `-1`. The same FFIEC payload carries `institutionId2017`, which **is** the institution's RSSD. Tested against FDIC `FED_RSSD` for the 18 highest-volume WI+IL lenders: **6 of 6 FDIC-insured banks matched exactly** (JPMorgan Chase 852218, US Bank 504713, BMO Harris 75633, Huntington 12311, Fifth Third 723112, Associated 917742). The 12 non-matches are credit unions and independent mortgage companies, which correctly hold no FDIC certificate. | Script 04 becomes an **exact ID join**: `CERT → FED_RSSD == institutionId2017 ← LEI`. No name matching, no city disambiguation, no manual adjudication. `match_method` and `match_quality` are still carried, because the non-bank lenders genuinely do not resolve and that must be visible rather than silent. | Gate 5.4 unblocked and BQ-4 is safe. Also narrows the working set usefully: **1,431 LEIs actually lend in WI+IL**, against 4,789 filers nationally. |
| 2026-08-14 | HMDA filers | Johnson Financial Group returned zero hits in the filer list and was nearly dropped as a subject candidate. The institution files as **"Johnson Bank"** (FDIC CERT 20296, Racine WI, $7.0B assets, RSSD 58243, active; HMDA LEI `IWRZQFYIRJ0IMURZBB68`). | Reinstated as a candidate. Selection criterion 4 must be applied against **name variants and the loan-level file**, never against a single exact-string search of the filer list. | A legal-entity name and a filing name differ routinely. Dropping a candidate on one failed string match would have silently narrowed the shortlist on a false negative. |
| **⛔ RETRACTED 2026-08-14** | ~~FDIC SOD~~ | **⛔ DO NOT CITE THIS FINDING. IT IS FALSE.** ~~*Illinois +506 in 2023 explained — charter consolidation. The increase concentrates in BMO Harris (CERT 16571), 519 → 1,045 branches.*~~ **The jump never happened.** It was an artifact of `01_download.py` filtering on `STALP` (charter state) instead of `STALPBR` (branch location) — BMO's Bank of the West acquisition is a **western-US** event that barely touches this footprint. In the corrected extract BMO goes **345 → 346** across that transition. | Withdrawn in full. Superseded by the two entries below. | Retained rather than deleted, because a retraction is part of the record — **but the retraction has since been missed by a reader who cited the withdrawn finding**, so the status markers above are deliberately unmissable. A struck-through entry that still reads as a finding at a glance is a documentation defect, not a preserved history. |
| 2026-08-14 | FDIC SOD | **DEFECT — the SOD extract was the wrong population.** `01_download.py` filtered on `STALP`, which is the **institution's charter state**, not `STALPBR`, which is where the **branch physically is**. Two failures in opposite directions: (a) every branch of an in-state-chartered bank was pulled *nationwide* — the "2023 Illinois" file spanned **32 states**, including 239 California and 183 Wisconsin branches, and BMO Harris contributed 1,045 rows of which only **181 were in Illinois**; (b) every branch located in the footprint whose bank is chartered elsewhere was **missing entirely** — JPMorgan Chase alone has **263 Illinois branches** that were absent. | **Fixed.** Filter changed to `STALPBR`, all 14 files re-downloaded. `02_profile_sod.py` now hard-fails if any row falls outside WI/IL by branch location, so this cannot recur silently. | Severe while it lasted. It corrupted every branch count, invented the Illinois anomaly, and would have broken **BQ-1 competitor saturation** outright, since competitor branches are precisely the out-of-state-chartered rows that were missing. The extract looked plausible — right order of magnitude, wrong rows — which is why nothing downstream caught it. |
| 2026-08-14 | FDIC SOD | **The Illinois 2023 anomaly does not exist.** Under the corrected `STALPBR` filter, Illinois goes **3,786 → 3,715** in 2023 — a *decline* of 71. The footprint declines monotonically in every year: **6,138 → 5,263 branches (−875, −14.3%)** across 2019–2025, with closures exceeding openings in all six transitions. | No anomaly to investigate. The consolidation narrative is stronger for being uniform rather than lumpy. Corrected totals: **39,315 branch-years**, not the 31,461 previously reported. | The real post-2020 consolidation story was visible all along and was being obscured by the defect, not revealed by it. |
| 2026-08-14 | FDIC SOD | **Branch transfers quantified.** Transitions computed two ways: globally on `UNINUMBR`, and on (`CERT`, `UNINUMBR`). `UNINUMBR` persists when a branch changes owner, so the difference isolates transfers from true openings and closings. | Over 2019–2025: **1,206 true closures, 331 true openings, and 625 branches that changed hands.** Reconciles exactly to the −875 net. Reported as a `transferred` column in `docs/sod_content_profile.md`. | Makes the consolidation claim defensible: branch counts alone cannot distinguish a closure from a sale. This is also the measure selection criterion 3 is now scored against. |
| 2026-08-14 | HMDA crosswalk | Verification of the `institutionId2017` join against the 2017 ARID risk — that the field is agency-code + respondent-ID, where respondent ID meant FDIC cert for FDIC-supervised banks, charter number for OCC, RSSD for Fed. A join that stripped the prefix and matched the remainder against `CERT` would mis-resolve national banks *and look clean*. | Tested across 120 highest-volume WI+IL lenders. The join matches the **full** value against `FED_RSSD`, never against `CERT`, so the trap does not apply — but it is real: **3 of 120 `institutionId2017` values also match some CERT**. Name agreement on all 38 RSSD resolutions. Associated Bank (OCC-chartered) verified end-to-end against an independent source: GLEIF returns "Associated Bank, National Association", Green Bay US-WI, matching FDIC at RSSD 917742 / CERT 5296 / $45.5B, with 9,260 applications = 203 per $B of assets. | Join confirmed sound. **Never key this on CERT.** |
| 2026-08-14 | Institution selection | **Byline Bank (CERT 20624) fails the volume sanity check.** $9.9B in assets, 48 Illinois branches, but only **56 mortgage applications** in the 2025 WI+IL file — 6 per $B of assets, against 203–588 for every other candidate. | **RESOLVED — a real business characteristic, not a bad match.** Three confirmations: GLEIF returns the registrant as "Byline Bank", Chicago US-IL, ACTIVE — the bank itself, not a holding company; no other Byline-named filer exists, so there is no separately-registered mortgage affiliate; and **all 56 records carry the HMDA partial-exemption flag**, which applies only to institutions originating fewer than 500 closed-end mortgages in each of the two preceding years. The regulation itself classifies Byline as a low-volume residential lender. Byline is a Chicago commercial and SBA lender. | The match is correct. **Byline remains a poor subject regardless** — BQ-4 needs enough mortgage volume for tract-level capture rates to be meaningful, and 56 applications across ~4,800 tracts is not. Note the loan-purpose mix is ordinary (52% home purchase, 36% refinancing), so purpose skew was *not* the confirming signal; exemption status was. |
| 2026-08-14 | AC-01 | **The acceptance criterion named a reference that measures a different population.** AC-01 reads "branch deposits summed by state match FDIC published state totals within 0.1%", which most naturally points at FDIC's `/banks/summary` endpoint. That endpoint reports deposits of institutions **headquartered** in a state, from Call Reports; SOD allocates deposits to the **branch's** location. For 2024: Wisconsin 128.6B published vs 193.4B ours (**+50%**), Illinois 595.1B vs 686.7B (**+15%**). | Reconciled instead against FDIC's own **SOD** server-side aggregate — same measure, same population — fetched as a separate artifact (`data/raw/fdic_sod_state_totals.csv`, SHA-256 in the manifest) and loaded as `ref_sod_state_totals`. | Using the summary endpoint would have reported a 50% failure that was really a definitional mismatch. **Fifth instance of the wrong-population shape**, and the first where the wrong population was written into an acceptance criterion rather than into code. The reconciliation is not circular: FDIC computes their figure in their engine over the full source table, ours travels paging → CSV → staging → type coercion → warehouse load → unit conversion. |
| 2026-08-14 | Scope | **Both geographic scope filters now tested against an independent signal, and the generalisation is worth stating: filtering on a metadata field can admit rows whose actual geography contradicts it.** The WI+IL scope rests on `state_code` for HMDA and `STALPBR` for SOD. `STALPBR` already failed once — the original filter used `STALP`, the charter state — and `state_code` has now been caught admitting an Alabama tract. | SOD is cross-checked by point-in-polygon against TIGER (31 disagreements enumerated); HMDA is cross-checked by referential integrity against `dim_tract` (1 orphan enumerated). Neither filter is trusted on its own. | A metadata field asserts where a row belongs; geometry demonstrates it. Where both exist, they should be compared rather than one believed. |
| 2026-08-14 | HMDA | **An Alabama tract in a Wisconsin–Illinois extract, caught by a referential check.** `fact_tract_lending` carries 2 rows for tract `01125010405` — state FIPS `01`, Tuscaloosa County, Alabama — 3 applications from a single lender. The source records carry `state_code = 'IL'`, a **blank** `county_code`, and an Alabama census tract. The HMDA API filters on `state_code`, so the record rode in on a field that disagrees with its own geography. | Enumerated in `docs/warehouse_load.md` rather than counted. The rows cannot join to `dim_tract` because no Alabama tract exists there, which is correct — they are excluded from tract-level analysis by construction and recorded rather than dropped silently. | 3 of 693,033 applications (0.0004%); the subject is unaffected and analytical impact is nil. **The point is what caught it.** The GEOID is a well-formed 11 characters and passes every column-level assertion — length, type, prefix. Only the join found it. A length check catches type damage; a referential check catches the consequence, and also the case where types are fine and the populations simply do not line up. |
| 2026-08-14 | Tooling | **Fourth occurrence of the project's recurring failure shape, and the first not involving data at all.** The tie-break sensitivity block in script 08 was guarded on `DEPSUMBR` being present in `dim_branch`. It is not. The comparison skipped, the report section never rendered, and the script exited 0. | Deposits now read from the staged SOD, and an empty result raises rather than passes. General rule added to the trap list: **a conditional that silently skips is indistinguishable from one that ran and found nothing** — guards on optional work must raise or log when they do not fire. | Caught by reading the output, not by trusting the return value. Four for four on the same shape: plausible output, wrong or absent population, no error raised. The others were the `STALP` branch filter, the LMI tract-versus-household basis, and `lmi_flag` NA collapsing to `False`. |
| 2026-08-14 | FDIC SOD | **A latest-only validity check passed a branch with four vintages of null-island coordinates.** BMO Harris (UNINUMBR 557506, Chicago) reports exactly `(0,0)` for 2019, 2020, 2021 and 2022, and a valid position in 2025. Script 07 validated only the most recent observation, so the branch passed. The defect surfaced only because a derived statistic was absurd — a 6,090-mile "relocation". | **Fixed: validity now runs on every vintage, not just the latest.** Invalid vintages are excluded from drift measurement. `dim_branch` carries `invalid_vintages` so the condition is queryable. | **6,090 miles is loud. The same defect in the current vintage would have been silent** — a Wisconsin branch placed in the Gulf of Guinea, joining to no tract and quietly dropping out. The general form is now in the trap list: **validity checks must run at the grain the data arrives in, not the grain it is consumed at.** |
| 2026-08-14 | FDIC SOD | **Coordinate drift classified by address change, not by distance or monotonicity.** 6,121 branches appear in 2+ vintages: 5,138 stable, **584 re-geocodes** (position moved, address identical), **399 relocations** (address changed). A monotonic-movement test misclassifies the largest case outright — Associated's Fond du Lac branch shows a 7.39-mile shift (211% of its tier radius) while reporting `W6606a Highway 23` in all seven vintages. It moved once and stayed, so it looks exactly like a relocation. | Addresses are normalised before comparison (`105 West Main Street` ≡ `105 W Main St`), because abbreviation conventions drift across vintages for reasons unrelated to anything moving — a systematic error, not a one-off. `dim_branch` carries `position_drift_miles`, `drift_kind` and `address_changed`. **Only true relocations above 25% of tier radius are flagged** — 2 of the subject's 232 branches (Waukegan 106%, Aurora 65%). | Latest position is used throughout; no branch splits into two entities, since at 65% and 106% of radius the old and new catchments still overlap substantially and splitting would cost `fact_branch_deposits` its stable key. **Flagging the four re-geocodes would inject a false explanation into BQ-3** — an outlier explained by something that never happened is worse than an unexplained one. |
| 2026-08-14 | FDIC SOD / TIGER | **AC-02 passes, and that is not the same as the join being right.** 6,467 distinct branches; **zero missing coordinates** except one, which makes the obvious presence check uninformative. The failure mode that survives is a branch with plausible-looking coordinates landing somewhere it should not — it still resolves to exactly one tract, so AC-02 passes and nothing downstream notices. | Added a cross-check between two independent statements of location: the tract the **coordinates** fall inside, and the county **FDIC reports** in `STCNTYBR`. Also four validity tests before the join — null island `(0,0)`, transposed lat/long, out-of-range, missing. | **31 branches disagree.** Every one of them passes AC-02. |
| 2026-08-14 | FDIC SOD | **The 31 county mismatches, characterised.** 30 of 31 involve *adjacent* counties — median distance from the reported county is 0.54 miles, and 18 are within 1 mile of the line, i.e. geocoding precision at a boundary. Four sit more than 5 miles out, one of them **190 miles**: LincolnWay Community Bank at "1621 North Ashland Avenue, **Chicago**", which FDIC reports in Edgar County while the coordinates place it in Cook. | **The coordinates are taken as authoritative; `tract_geoid` comes from the spatial join.** In every case far enough out to check by hand, the branch's own address text agrees with the coordinates rather than with `STCNTYBR` — "Chicago" → Cook, "West Chicago" → DuPage. Several collar-county branches are reported as Cook, which reads as a reporting convention rather than scattered typos. `county_agrees` is retained on every row of `dim_branch` so any analysis keyed on FDIC-reported county can exclude them. | Not concentrated in any institution (maximum 2 each) and **the subject is unaffected — 0 of Associated's branches**. Immaterial to the current analysis, but it is the exact class of error AC-02 was assumed to cover and does not: a branch in the wrong market contributes its deposits to the wrong market and pulls its catchment over the wrong demographics. |
| 2026-08-14 | HMDA | **`action_taken` verified before aggregation, not after.** Eight distinct outcomes share one column. Counting originations loosely overstates them by **+18.9%** if purchased loans (code 6) are included, or **+81.0%** with no filter at all. | `fact_tract_lending` keeps `action_taken` **in the grain** rather than pre-aggregating to origination and denial counts, so no downstream query can count originations without naming `action_taken = 1`. Measures are deliberately minimal — `record_count`, `total_amount`. Code-to-meaning mapping is in the data dictionary and the script fails on any unmapped code. | **The distortion is not uniform, which is worse than a simple overstatement.** Associated is 0.8% purchased loans; several lenders in this file are 88–99%, one showing 1,252 purchased against 13 originated — secondary-market aggregators, not local lenders. Including code 6 would inflate competitors by roughly two orders of magnitude more than the subject, make Associated's capture rate look far worse than it is, and drive BQ-4 toward unmet demand that does not exist. Excluding it costs 8 tracts of 4,773. |
| 2026-08-14 | HMDA | **7,863 records (1.12%) carry no census tract** and cannot enter a tract-grain fact table. Composition: **42.9% are preapprovals** — 50.3% of all code-7 and 71.6% of all code-8 records lack a tract, which is structural, since a preapproval often has no property yet. Only 17.1% are originations, amounting to **0.3% of all originations**. Denials are over-represented: **1.9% of denials lack a tract against 0.3% of originations**. Only 907 of the 7,863 carry even a county code. | Excluded from `fact_tract_lending` — a row with no tract cannot occupy a tract grain. Recorded here rather than dropped silently. **The subject is entirely unaffected: 0 of 9,260 Associated records lack a tract.** | Low risk for BQ-4, which measures origination capture. The denial over-representation would matter for any tract-level denial-rate analysis and must be stated there. Separately, **three lenders report no geography on 100% of their records** (751, 706, and 278 records) — worth noting if any competitor-level attribution depends on them. |
| 2026-08-14 | Catchments / BQ-6 | **The LMI over-indexing finding was measured over the wrong population and is ~40% smaller than first reported.** Initially computed as LMI *tract count* inside catchments vs. across WI+IL: 33.2% vs 29.9%, +3.3pp. But LMI tracts are median **0.58 sq mi and 2,091 households/sq mi**, against **2.89 sq mi and 594/sq mi** for non-LMI — five times denser, five times smaller, and with *fewer* households each. Counting tracts over-weights them. | Re-measured on households: **27.3% inside catchments vs 25.4% across the two states, +1.9pp.** The finding holds but is mild. The executive summary must state the household figure, and may show the tract figure only as the naive comparison it is. | Same class of error as the `STALP` filter — a plausible number computed over the wrong population. Reported before it hardened into a claim. Context: only **39.7% of WI+IL households** fall inside any Associated catchment. |
| 2026-08-14 | Catchments | **Radius rule set: computed, not chosen.** Radius = observed median nearest-neighbour distance between the subject's own branches, per tier. On the 2025 vintage: 3.5 / 13.1 / 20.8 miles (metro / micro / rural). Tiering moved from binary CBSA-membership to three-way — micropolitan spacing is **3.7× metro**, so the binary rule was giving branches 13 miles apart a 3-mile radius. | `config/catchment.yaml` now states the rule as the artifact and records the observed values as *evidence, not input*. Script 08 must recompute every run. | A chosen radius cannot answer "why 3.5 and not 4?". A computed one has no free parameter. Recomputation is required for FR-06: a denser future branch set must move the radius, or the tool goes quietly wrong on a new vintage in the same way the binary tier rule was wrong on this one. **Noted for the case study: the widest scheme tested produced the best LMI coverage (56.9%), and that is precisely why it was not chosen — selecting a radius to improve the equity number would be reverse-engineering BQ-6.** |
| 2026-08-14 | FDIC SOD | **FR-06 footprint guard, built and immediately productive.** The refresh procedure promises the retail team can re-run against a new vintage unattended. Added a guard to `02_profile_sod.py` that halts if a vintage introduces branches outside the configured scope, or if any institution with ≥10 branches moves more than 20% year-over-year. On first run it halted on two 2024→2025 movements. | Both investigated before being accepted. **CERT 24998 Commerce Bank** 14→10: organic closures, steady decline across the whole window, institution still active — accepted. **CERT 58458 HTLF Bank** 17→0: FDIC lists the certificate inactive and **every one of its 2024 branch numbers reappears in 2025 under CERT 8273, UMB Bank** — the UMB/Heartland acquisition. Recorded with findings in `config/footprint_acknowledged.json`. | The HTLF transfer must be read as 17 branches changing hands, not 17 closures, or the consolidation rate is overstated. It also explains why UMB appears in the two-state field with only 18 branches — its entire WI+IL presence is one year old and acquired. **Built for a known future break:** Associated closed American National on 2026-04-01 and was converting branches in Q3 2026, so the 2026 vintage will carry Omaha and Twin Cities branches inside Associated's certificate. The tool will now refuse to run rather than absorb that silently. |
| 2026-08-14 | Warehouse | Institution attributes come from the FDIC API at *current* vintage while branch and deposit data are as of 30 June of the SOD year. For the subject these now straddle a completed acquisition: the asset figure is post-American-National, the branch network is pre. | `dim_institution` gains a required `attribute_as_of_date`. Any ratio with an institution-level denominator over a branch-level numerator must confront the mismatch rather than span it. | Same shape as the `STALP` defect — plausible magnitude, wrong population. A live division error rather than a footnote. |
| 2026-08-14 | Institution selection | **Criterion 7 checked manually — Associated Banc-Corp is NOT in a pending merger.** It announced the acquisition of American National Corporation on 2025-12-01, received OCC and Federal Reserve approval 2026-03-12, and **closed 2026-04-01** ($604M, all-stock). Nothing pending as of 2026-08-14. | **Passes.** The deal closed after the 2025-06-30 SOD as-of date, so no American National branch appears under Associated's certificate in this analysis, and American National is an Omaha/Twin Cities franchise — Nebraska and Minnesota, outside the footprint. | Three carries for the case study: the 2019–2025 window is unaffected; the geography is outside WI+IL so the analysis is undisturbed; but **institution-level figures are now mixed-vintage** — the asset total is post-acquisition while branch and deposit data are pre-acquisition. Band cleared either way. Branch conversion was scheduled Q3 2026, so **the 2026 SOD vintage will show a discontinuity** — anyone re-running against a newer vintage must know that before reading the trend. |
| 2026-08-14 | Institution selection | **The criteria did not winnow a field — they left exactly one candidate.** Recorded explicitly so the case study cannot present a seven-criterion framework as though it ranked genuine contenders. | Stated plainly in `docs/institution_selection.md` with a per-criterion elimination count. The convergence is **structural, not arbitrary**: only **9 of 699** institutions have a real two-state footprint, and 8 of those 9 are national or super-regional banks (JPMorgan $4,017B, Wells Fargo $1,852B, US Bank $683B, PNC $568B, Huntington $284B, BMO $252B, Old National $72.6B, UMB $72.3B) whose deposits book to headquarters at scale. Associated at $45.5B is the only mid-size regional. | Documented contingency, since a single candidate is a single point of failure: the fallback is **Old National Bank** (CERT 3832, $72.6B), requiring criterion 6 to relax to ~$75B *and* the criterion 3 segmentation clause, since its First Midwest acquisition is visible in this data as a 109-branch movement in 2021→2022. UMB is not a fallback — 18 branches fails criterion 2. |
| 2026-08-14 | Institution selection | Criterion 1 ("branches in both WI and IL") passes on a single branch. Three of five candidates hold a token presence in their second state: North Shore 1 Illinois branch, Byline 1 Wisconsin branch, First American 3 Wisconsin branches. | Flagged for a decision. A one-branch presence satisfies the letter of criterion 1 but not its intent — the multi-state comparison in BQ-1 and BQ-2 needs a real footprint on both sides. Suggest a floor of 5 branches per state, or ranking on the smaller state's count. | Only **Associated Bank** (137 WI / 30 IL) has a genuinely two-state footprint under any reading. |
| 2026-08-14 | HMDA crosswalk | **CORRECTION to the entry below: the missing rate is 15.3%, not ~9%.** Measured across all 1,429 filers lending in WI+IL rather than the 120-lender sample: **218 have no usable `institutionId2017`**. | Same handling — explicit `match_quality`, never a null. The index is cached to `data/staging/lei_rssd_index.json` so the ~1,400 lookups run once. | The sample estimate was low by two thirds. Where the full population is cheap to measure, measure it rather than extrapolating from the head of a distribution. |
| 2026-08-14 | Tooling | Python's `Path.write_text()` defaults to the platform encoding, which is **cp1252 on Windows**, and raised `UnicodeEncodeError` on `≥` when writing a report. `docs/sod_content_profile.md` had escaped this only by luck — em-dashes exist in cp1252 and `≥` does not. | All `read_text`/`write_text` calls in scripts 01–03 now pass `encoding="utf-8"` explicitly. | The same trap as the PowerShell round-trip, in a different tool. Any generated document containing a character outside cp1252 would fail on Windows and succeed elsewhere. |
| 2026-08-14 | HMDA crosswalk | **`institutionId2017` is missing for ~9% of filers** — 11 of 120 probed return empty or `-1`. Institutions chartered after 2017 have no 2017 ARID to carry. | Script 04 must assign these an explicit `match_quality` of `no_2017_id` rather than leaving a null. Unresolvable is a finding; a null is an accident. | Silent dropout otherwise. A newer bank would simply vanish from the crosswalk with nothing recording why. |
| 2026-08-14 | Documentation | An earlier count of 30,461 SOD branch-year rows was published in the README and in commit messages. The correct figure is **31,461** (14 state-year files, verified on load). | README corrected. Commit messages left as written — rewriting history to fix a transcription error is not worth the cost. | Portfolio numbers are checkable. A wrong one in the README is worse than no number. |
| 2026-08-14 | HMDA | The full loan-level file is 266 MB, over GitHub's 100 MiB hard limit, so it cannot be committed for the durability argument in 05 §1. A 21-of-99-column subset was still 95 MB — under the limit, but only just, and the next vintage would exceed it. | Committed subset is **gzipped**: 12.1 MB, all 700,896 rows and 21 columns preserved. pandas and DuckDB read `.csv.gz` directly. The full file stays local, excluded via `.gitignore`, with its SHA-256 pinned in the manifest. **Columns dropped, rows never dropped** — row filtering would need to be asked about and would break the reproducibility of the script 06 aggregation. | Applicant-level demographics were deliberately excluded from the committed subset. They are not index inputs, the equity check runs on income class rather than race (05 §4), and there is no reason to publish applicant characteristics to answer BQ-4 or BQ-6. |
| 2026-08-14 | SQL-11 / HMDA | **Ranking on absolute unmet demand selects a shortlist that is 2.0% LMI against a 29.8% footprint baseline** — a fifteen-fold under-representation produced by a ranking containing no income or race term. The mechanism is scale, not bias in the data: origination *volume* tracks affluence and tract size (spearman **+0.473** against median family income, **+0.704** against households), and the median LMI tract records **41** originations against **87** in the median non-LMI tract, so absolute unmet demand is mechanically lower in LMI tracts however underserved they are. LMI share of the top 50 by basis: absolute **2.0%**, uncaptured applications **4.0%**, per household **4.0%**, per application **50.0%**. | **No basis adopted.** All four emitted as columns (`basis_absolute`, `basis_per_household`, `basis_per_application`, `basis_uncaptured_apps`); default ordering left on absolute *because* it is the conventional commercial reading, with the header stating that this is precisely why it cannot be used silently. `unmet_per_application` was **not** adopted on the strength of recovering LMI representation — a measure chosen because it improves the equity number is reverse-engineered, the same error the catchment radius rule avoided. The two bases answer different questions and both ship: absolute is a **volume** question (right for BQ-5 siting), per-application a **penetration** question (right for examiner-facing review). BQ-5 must name its basis; BQ-6 must report the composition of *that* basis, not another. | This is the 05 §4 redlining risk arriving through the back door — not by weighting income, but by ranking on a volume measure correlated with it. Third instance of a ratio inheriting a scale relationship from its denominator (after the BQ-3 performance index), and the first to land on fair-lending exposure. Defending absolute as the BQ-5 default makes BQ-6 **harder**: 2.0% must be reported and addressed, not noted. The intended output is both rankings presented as a tension rather than resolved into one number. |
| 2026-08-14 | ACS 2024 5-year | **17 tracts report `median_hh_income` of exactly $250,001** — the ACS **top-code**, a censored bound meaning "at least $250,001", not a measurement. The lower extreme, **$2,499** in an 802-household tract, is doing the same job at the other end. `config/index_weights.yaml` sets `min_max` normalisation, which anchors the entire scale on both. | Logged, **not silently changed** — switching normalisation alters every index score and is a weights-file decision. Options recorded in the audit: `z_score` (already supported, does not anchor on extremes) or winsorising before min-max. Tracts left as reported. | A default standing in for a state, in the **normalisation step** rather than in the data, which is why no null check finds it. Every tract's normalised income is currently a fraction of a distance to a number that was never measured. Tracts in the bottom 2% of income also carry a median 824 households against 1,517 overall (0.55×), so the small-sample tail effect is real at the bottom, mild at the top (0.90×). |
| 2026-08-14 | FDIC SOD | County-grain deposit CAGR — a 25%-weighted index component — is **dominated by booking-centre and institution-level events**, and the two most extreme counties in the footprint are both artifacts. **McLean County IL, −16.9%**: State Farm Bank, F.S.B. held **$11.39bn across 2 branches** in 2020 (76% of county deposits) and is absent from 2021. **Brown County WI, +12.3%**: the **subject's own HQ booking**, $2.59bn → $7.38bn. | Flagged before SQL-12 consumes it; component **not yet used**. Same treatment as the BQ-1 HQ exclusion will be required — the growth measure needs booking-centre branches excluded or the component computed on a retail-deposit basis. | Brown County is the HQ circularity arriving through a component: the index would reward siting in Brown County **because the subject books deposits there**. McLean is the mirror image — the largest deposit market in the footprint read as its worst, on an event that says nothing about local demand. **Neither was findable by correlation testing**; the component passed its scale test cleanly (pooled +0.001) and is still unusable in this form. The audit's prediction of *small-county volatility* was also wrong in direction: sd 0.0256 in the smallest deposit quartile against 0.0407 in the largest, 0.63×. |
| 2026-08-14 | ACS acquisition | **Only one ACS vintage was ever acquired** (`acs5_2024`), so `household_growth` — 20% of the opportunity index — has no second point to compute growth from and cannot be built. `dim_tract` carries no year dimension. Separately, `competitor_saturation` (a further 20%) has no committed source: `bridge_branch_catchment` covers **subject** branches only, 166 of 241. | **Raised, not worked around.** Acquiring a second non-overlapping 5-year vintage (2015–2019 against 2020–2024, the comparison Census itself sanctions) or dropping the component and redistributing its weight both need a decision — the latter changes `config/index_weights.yaml`. Saturation figures used in testing are provisional, computed at script 08's catchment definition. | 40% of index weight has no warehouse source. Found by attempting to scale-test the components rather than by any check — the gap is invisible until something tries to read the series. |
